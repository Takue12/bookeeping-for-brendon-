{
  "name": "nexa-ledger-static",
  "version": "1.0.0",
  "description": "Personal Bookkeeping Services Website",
  "scripts": {
    "build": "parcel build src/index.html --dist-dir public --no-source-maps",
    "dev": "parcel src/index.html --port 3000",
    "clean": "rm -rf dist public .parcel-cache"
  },
  "dependencies": {
    "parcel": "^2.12.0"
  },
  "devDependencies": {
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3"
  },
  "browserslist": [
    "> 0.5%",
    "last 2 versions",
    "not dead"
  ],
  "staticFiles": {
    "staticPath": "public"
  }
}
